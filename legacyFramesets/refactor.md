1. ObjectiveRefactor a legacy HTML file using the obsolete <frameset> element to a modern HTML5-compliant structure using <iframe> and CSS Grid. You must maintain the exact layout proportions, scrolling behaviors, and target frame interactions.


2. Input Data (Legacy Source) index.html is the original source code that needs to be refactored

3. Strict Refactoring Rules & Specifications3.1. Layout & Styling (CSS Grid)Viewport Setup: Reset html and body with margin: 0; padding: 0; height: 100%; overflow: hidden; to prevent double scrollbars.Proportions: Use CSS Grid on a container element to mimic rows="100,*,50". The track configuration must be grid-template-rows: 100px 1fr 50px;.Sizing: The grid container must occupy the full viewport width and height (width: 100vw; height: 100vh;).Frame Removal: Set border: none; width: 100%; height: 100%; display: block; on all <iframe> elements to remove default borders and fill the grid cells.3.2. Behavioral Attributes Replacementname="..." (Crucial): Keep the exact name attributes (header, content, footer) on the new <iframe> elements. This ensures existing <a href="..." target="content"> links in header.html continue to work without changing the source of the embedded pages.scrolling="no": Apply overflow: hidden; to the header and footer iframes.noresize: Do not use resizable CSS handles. The static pixel row definitions (100px, 50px) natively prevent resizing.<noframes>: Delete the <noframes> block entirely as modern browsers do not require it.