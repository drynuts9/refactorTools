Rules:
- Never suppress warnings with @
- Prefer ?? over isset ternary
- Prefer local variables over mutating $_POST
- Use "" for text fields
- Use null for optional numeric fields
- Preserve original behavior